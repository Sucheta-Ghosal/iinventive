version: "3"

services:

  frontend:
    build: .
    ports:
      - "5173:80"
    depends_on:
      - backend

  backend:
    build: ./server
    ports:
      - "5000:5000"

  database:
    image: mongo
    ports:
      - "27017:27017"
